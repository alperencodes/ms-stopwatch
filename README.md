# Beatmap Millisecond Stopwatch

A simple, dependency-free stopwatch for creating rhythm-game beatmaps with millisecond-accurate timestamps.

Open `index.html`, start the timer, and press **Space** to record notes. Stop the timer, enter the song details, and save the beatmap as JSON. Recorded notes include an empty `lane` value to fill in later.
